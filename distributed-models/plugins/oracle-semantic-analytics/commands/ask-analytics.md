---
description: Ask an Oracle warehouse analytics question. Routes to the right semantic model, generates and validates SQL, and optionally executes against Oracle.
---

Use the `oracle-analytics-router` skill to handle this request.

All bundled files are under `${CLAUDE_PLUGIN_ROOT}`. Do not search the user's project folder for plugin files.
